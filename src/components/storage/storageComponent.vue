<script setup>
const props = defineProps({
  id: {
    type: Number,
    required: true,
  },
})
</script>
<template>
    <h1>{{ props.id }}</h1>
    <h1>{{ props.id }}</h1>
    <h1>{{ props.id }}</h1>
    <h1>{{ props.id }}</h1>
    <h1>{{ props.id }}</h1>
</template>